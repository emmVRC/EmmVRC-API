// const socket = require('socket.io')();

// const component = {
//     debug: require('./debug/socket'),
// }

// // bind the path to the router 
// // namespace/endpoint

// for (const [route, endpoint] of Object.entries(component)) {
//     socket.of(route).on(endpoint);
//     Log.writeLog("[ PROTECTED ] Socket Module Loaded: {0}", route);
// }

// module.exports = socket;